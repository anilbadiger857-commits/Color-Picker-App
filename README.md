# Color-Picker-App
Kiro A simple color picker app that updates the background and displays a color box when you select colors. Selected colors are automatically saved to localStorage and displayed as clickable swatches for quick reuse.
